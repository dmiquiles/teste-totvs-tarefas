import { Routes } from '@angular/router';
import { ErrorComponent } from './error/error.component';

export const ERRO_ROUTES: Routes = [
    {
        path: '',
        component: ErrorComponent
    }
];
